import { Story } from '@storybook/react';
import React from 'react';
import LayoutManager from '../../src/components/layout-manager/layout-manager';

export default {
  title: 'Layout Manager',
};

export const layoutManager = () => <LayoutManager />;
