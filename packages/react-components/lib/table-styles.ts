import { makeStyles } from '@material-ui/core/styles';

export const TableStyles = makeStyles({
  table: {
    backgroundColor: '#ddd',
  },
  tableHeadRow: {
    backgroundColor: '#eee',
  },
  tableHeadCell: {
    fontWeight: 'bold',
  },
});
